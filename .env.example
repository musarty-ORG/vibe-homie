# Admin Mode
# Set to 'true' to enable admin mode when ?admin=1 is in the URL
# When enabled with the query parameter, shows the Sandbox Remote Filesystem and Logs panels
# ADMIN_MODE_ENABLED=true

# Other environment variables (configure as needed for your environment)
